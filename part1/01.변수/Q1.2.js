/**
 * Q1.2: 여러 가지 타입 변수를 선언하고 각 변수에 값을 할당해보세요.
 *
 * let num; -> 선언 후, number 타입 값을 num에 할당해주세요.
 * let str; -> 선언 후, string 타입 값을 str에 할당해주세요.
 * let bool; -> 선언 후, boolean 타입 값을 bool에 할당해주세요.
 * let arr; -> 선언 후, array 타입 값을 arr에 할당해주세요.
 * let obj; -> 선언 후, object 타입 값을 obj에 할당해주세요.
 * let undef;-> 선언 후, undefined 타입 값을 undef에 할당해주세요.
 * let nul; -> 선언 후, null 값을 nul에 할당해주세요.
 */

let num;
let str;
let bool;
let arr;
let obj;
let undef;
let nul;

// export 를 수정하지 마세요.
export { num, str, bool, arr, obj, undef, nul };
