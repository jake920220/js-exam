/**
 * Q1.5: 호이스팅(Hosting)
 *
 * - console.log(d); var d = 5; 실행 시, 실제로는 undefined를 출력한 뒤 정상 실행됩니다.
 * - 아래 함수 hoistingTest()에서 이 로직을 구현해보세요.
 * - 실행 결과, 테스트 코드에서 'undefined'를 출력했는지 여부를 확인합니다.
 */

function hoistingTest() {
  // TODO: console.log(d);
  //       var d = 5;
  // 실제로 실행했을 때, console 창에 undefined가 찍히도록 작성.
}

// export 를 수정하지 마세요.
export { hoistingTest };
