/**
 * Q2.3: 단축 연산자 (+=, -=, *=, =* 등)
 * - 단축 연산자를 사용해 increment, decrement 변수를 1씩 증가/감소하세요.
 * - 단축 연산자를 사용해 add, subtract, multiply, divide 변수를 num1을 이용해서 더하기, 빼기, 곱하기, 나누기 연산을 수행하세요.
 */

let increment = 0;
let decrement = 0;

// TODO: 단축 연산자를 사용해 increment, decrement 변수를 1씩 증가/감소하세요.

const num1 = 10;

let add = 0;
let subtract = 20;
let multiply = 1;
let divide = 100;

// TODO: 단축 연산자를 사용해 add, subtract, multiply, divide 변수를 num1을 이용해서 더하기, 빼기, 곱하기, 나누기 연산을 수행하세요.

// export 를 수정하지 마세요.
export { increment, decrement, add, subtract, multiply, divide };
