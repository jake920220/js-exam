/**
 * Q4.3: loginCheck 함수
 *
 * - loginCheck 함수를 작성하세요.
 * - 파라미터로 id와 pw를 전달받아서, id가 "admin"이고 pw가 "1234"이면 "success"를 반환하고, 아니면 "fail"을 반환하세요.
 * - 중첩 if/else 를 사용해도 되고, 간단한 if문 2개 사용해도 됩니다.
 *
 * @param {string} id
 * @param {string} pw
 * @returns {string} "success" or "fail"
 */

// TODO: loginCheck 함수를 작성하세요.
function loginCheck() {}

// export 유지 (수정하지 마세요)
export { loginCheck };
