/**
 * Q5.2: skipAndStop 함수
 *
 * - skipAndStop() 함수를 작성하세요.
 * - 1부터 10까지 반복하면서, 홀수는 합하고 짝수는 건너뛰어 최종 합을 반환합니다.
 *
 * 예시 로직:
 *   1 -> 합(1)
 *   2 -> 짝수이므로 skip
 *   3 -> 합(1+3=4)
 *   4 -> 짝수이므로 skip
 *   5 -> 합(4+5=9)
 *   6 -> 짝수이므로 skip
 *   7 -> 7을 합(9+7=16)
 * 최종 합 = 16
 *
 * @returns {number}
 */

// TODO: skipAndStop 함수를 작성하세요.
function skipAndStop() {}

// export를 수정하지 마세요.
export { skipAndStop };
