/**
 * Q6.6: getObjectKeys 함수
 *
 * - getObjectKeys 함수는 파라미터로 obj 객체를 받습니다.
 * - Object.keys(obj)를 사용하여, obj의 "모든 직접 소유 키"를 담은 배열을 반환하세요.
 *
 * 예: obj = { name: "Alice", age: 25 } -> ["name", "age"]
 *
 * @param {object} obj
 * @returns {string[]}
 */

function getObjectKeys(obj) {
  // 여기에 코드를 작성하세요.
  // TODO: Object.keys()를 사용해 obj의 모든 키를 배열로 반환하세요.
}

// export를 수정하지 마세요.
export { getObjectKeys };
