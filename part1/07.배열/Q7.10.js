/**
 * Q7.10: reverseArray 함수
 *
 * - reverseArray(arr)를 작성하세요.
 * - 원본 배열을 뒤집지 않고, 뒤집힌 새 배열을 반환합니다.
 * - 원본 배열을 복사하고 뒤집은 후 반환합니다.
 *
 * @param {any[]} arr
 * @returns {any[]} 뒤집힌 새 배열
 */

// TODO: reverseArray 함수를 작성하세요.
function reverseArray() {}

// export를 수정하지 마세요.
export { reverseArray };
