/**
 * Q25: 인기 검색어 캐시 시스템
 *
 * - updateTopKeywords(keywords): 검색어를 많이 검색된 순으로 상위 10개 캐시에 저장
 *   - 중복 키워드는 1회만 노출
 * - getTopKeywords(): 현재 캐시된 상위 키워드 반환
 */

let topKeywordsCache = [];

function updateTopKeywords(keywords) {
  // TODO
}

function getTopKeywords() {
  // TODO
  return [];
}

// export 를 수정하지 마세요.
export { topKeywordsCache, updateTopKeywords, getTopKeywords };
