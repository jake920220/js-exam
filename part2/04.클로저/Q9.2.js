/**
 * Q9.2: 카운터 함수 만들기
 *
 * - createCounter 함수를 작성하세요.
 * - 내부에서 let count = 0; 으로 초기화
 * - increase, decrease, currentValue 3가지 메서드를 가진 객체를 리턴
 *   - increase(): count를 1 증가
 *   - decrease(): count를 1 감소
 *   - currentValue(): 현재 count 반환
 *
 * @returns {{ increase: Function, decrease: Function, currentValue: Function }}
 */

// TODO: createCounter 함수를 작성하세요.
function createCounter() {
  // TODO
}

// export를 수정하지 마세요.
export { createCounter };
