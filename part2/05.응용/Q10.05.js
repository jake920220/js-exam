/**
 * Q: Query String 파싱
 *
 * - `parseQueryString` 함수를 작성하세요.
 * - 예: "?search=apple&page=2" 형태의 쿼리스트링에서 key-value를 객체로 파싱하세요.
 * - 쿼리스트링은 항상 "?"로 시작한다고 가정합니다.
 * - 값이 없는 경우(예: "?search=")에는 빈 문자열("")로 처리하세요.
 * - ex) "?search=apple&page=2" => { search: "apple", page: "2" }
 * - 여러 쿼리 파라미터가 없거나 빈 문자열인 경우 빈 객체({})를 반환하세요.
 *
 * @param {string} queryString
 * @returns {object} 파싱된 쿼리 파라미터 객체
 */

function parseQueryString(queryString) {
  // TODO
}

// export를 수정하지 마세요.
export { parseQueryString };
