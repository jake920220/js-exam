/**
 * Q: Object에서 지정된 key만 추출
 *
 * - `pickKeys` 함수를 작성하세요.
 * - 첫 번째 파라미터 obj에서, 두 번째 파라미터로 받은 배열(keys)에 해당하는 key만 추출하여 새 객체를 반환합니다.
 * - 존재하지 않는 key는 무시하며, obj의 다른 key는 포함되지 않아야 합니다.
 *
 * 예)
 *   pickKeys({ a:1, b:2, c:3 }, ["b", "d"]) => { b:2 }
 *
 * @param {object} obj
 * @param {string[]} keys
 * @returns {object}
 */

// TODO: pickKeys 함수를 작성하세요.
export function pickKeys(obj, keys) {
  // TODO
}
